name: Litterbox Ubuntu dependencies
description: Install packages required to build AgentKit on Ubuntu runners

runs:
  using: composite
  steps:
    - name: Install system dependencies
      shell: bash
      run: |
        set -eux -o pipefail
        sudo apt-get update
        sudo apt-get install -y libdbus-1-dev
