[formatter.nixfmt]
command = "nixfmt"
includes = ["*.nix"]

[formatter.mdformat]
command = "mdformat"
includes = ["*.md"]

[formatter.toml-sort]
command = "toml-sort"
options = ["--in-place"]
includes = ["*.toml"]
