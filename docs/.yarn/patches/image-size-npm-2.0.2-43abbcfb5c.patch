diff --git a/dist/detector.cjs b/dist/detector.cjs
index 0898608dbeca36722dfd795341b1c3c1448f58aa..34e22bd9e6fb97a845ed1f6115c6a88f70902299 100644
--- a/dist/detector.cjs
+++ b/dist/detector.cjs
@@ -27,22 +27,36 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   const methodName = `readUInt${bits}${endian}`;
   return methods[methodName](input, offset);
 }
+const MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
diff --git a/dist/detector.mjs b/dist/detector.mjs
index 67afcab02d627f95e98c938499e07a934b832a26..e4ac41738df818b7e455a5a8a6e64af0e3d1acbb 100644
--- a/dist/detector.mjs
+++ b/dist/detector.mjs
@@ -25,22 +25,36 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   const methodName = `readUInt${bits}${endian}`;
   return methods[methodName](input, offset);
 }
+const MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
diff --git a/dist/fromFile.cjs b/dist/fromFile.cjs
index c226d3dac07f235db456c774b26cca88f655e1af..99273ebb41b8ee14090b0a62a47aea9ff74cd90d 100644
--- a/dist/fromFile.cjs
+++ b/dist/fromFile.cjs
@@ -53,22 +53,36 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   const methodName = `readUInt${bits}${endian}`;
   return methods[methodName](input, offset);
 }
+const MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
diff --git a/dist/fromFile.mjs b/dist/fromFile.mjs
index 4a3c44432982397204f7a9d04fc66ef65a8e7ba4..ad9262323ade70bda94d73fb72d72e032ede4487 100644
--- a/dist/fromFile.mjs
+++ b/dist/fromFile.mjs
@@ -30,22 +30,36 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   const methodName = `readUInt${bits}${endian}`;
   return methods[methodName](input, offset);
 }
+const MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
diff --git a/dist/index.cjs b/dist/index.cjs
index bde0210eb131db06ccca30a5c466c7252a03af50..71fc157e3fd034ad9b21090e43f22d6eaedb6633 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -29,22 +29,36 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   const methodName = `readUInt${bits}${endian}`;
   return methods[methodName](input, offset);
 }
+const MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
diff --git a/dist/index.mjs b/dist/index.mjs
index 22c17afad4e406eba013fff7858366cb5f8e3ba4..2265f451bd7e9eb348a68b8be5531d2510b25ad7 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -25,22 +25,36 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   const methodName = `readUInt${bits}${endian}`;
   return methods[methodName](input, offset);
 }
+const MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
diff --git a/dist/lookup.cjs b/dist/lookup.cjs
index 9731ad8bb0949f8fdf9297851bdb312e91282f01..b8c962dde30ab4fccf8a66ac31395ba6f509d86d 100644
--- a/dist/lookup.cjs
+++ b/dist/lookup.cjs
@@ -27,22 +27,36 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   const methodName = `readUInt${bits}${endian}`;
   return methods[methodName](input, offset);
 }
+const MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
diff --git a/dist/lookup.mjs b/dist/lookup.mjs
index f787d1c8408d7b681fec8617595d058f66959869..7ef546001b3ccbf31d62d0a334581cb3c1441b58 100644
--- a/dist/lookup.mjs
+++ b/dist/lookup.mjs
@@ -25,22 +25,36 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   const methodName = `readUInt${bits}${endian}`;
   return methods[methodName](input, offset);
 }
+const MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
diff --git a/dist/types/heif.cjs b/dist/types/heif.cjs
index 1f490788ea654ea9896060a5c0253d81e348af92..457068c7c831100edcdd88f2cf32414120c67583 100644
--- a/dist/types/heif.cjs
+++ b/dist/types/heif.cjs
@@ -5,22 +5,36 @@ var decoder = new TextDecoder();
 var toUTF8String = (input, start = 0, end = input.length) => decoder.decode(input.slice(start, end));
 var getView = (input, offset) => new DataView(input.buffer, input.byteOffset + offset);
 var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, false);
+const MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
diff --git a/dist/types/heif.mjs b/dist/types/heif.mjs
index 01330919e01b3c034a96a7b4b1dbf5a7f658bb2d..55327bb91f1fee936f760631306835963e86e424 100644
--- a/dist/types/heif.mjs
+++ b/dist/types/heif.mjs
@@ -3,22 +3,36 @@ var decoder = new TextDecoder();
 var toUTF8String = (input, start = 0, end = input.length) => decoder.decode(input.slice(start, end));
 var getView = (input, offset) => new DataView(input.buffer, input.byteOffset + offset);
 var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, false);
+const MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
diff --git a/dist/types/icns.cjs b/dist/types/icns.cjs
index 32c8d370dcf80a10f14a6deebfdbe04d3c70585c..02effe936080c8a5fc556ee8d00a80c5d949504b 100644
--- a/dist/types/icns.cjs
+++ b/dist/types/icns.cjs
@@ -8,6 +8,7 @@ var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, fa
 
 // lib/types/icns.ts
 var SIZE_HEADER = 4 + 4;
+var MIN_ENTRY_LENGTH = 8;
 var FILE_LENGTH_OFFSET = 4;
 var ENTRY_LENGTH_OFFSET = 4;
 var ICON_TYPE_SIZE = {
@@ -63,6 +64,7 @@ function getImageSize(type) {
   const size = ICON_TYPE_SIZE[type];
   return { width: size, height: size, type };
 }
+var MIN_ENTRY_LENGTH = 8;
 var ICNS = {
   validate: (input) => toUTF8String(input, 0, 4) === "icns",
   calculate(input) {
@@ -71,10 +73,17 @@ var ICNS = {
     let imageOffset = SIZE_HEADER;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
+      if (inputLength - imageOffset < MIN_ENTRY_LENGTH) break;
       const imageHeader = readImageHeader(input, imageOffset);
+      const entryLength = imageHeader[1];
+      if (entryLength < MIN_ENTRY_LENGTH) break;
       const imageSize = getImageSize(imageHeader[0]);
-      images.push(imageSize);
-      imageOffset += imageHeader[1];
+      if (imageSize.width && imageSize.height) {
+        images.push(imageSize);
+      }
+      const nextOffset = imageOffset + entryLength;
+      if (nextOffset <= imageOffset) break;
+      imageOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
diff --git a/dist/types/icns.mjs b/dist/types/icns.mjs
index 8710027bc5890a79d05a7fdb87de3018e556729e..17b4d5b3ee3c39e0e43c2d2c61c167787d2a1237 100644
--- a/dist/types/icns.mjs
+++ b/dist/types/icns.mjs
@@ -6,6 +6,7 @@ var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, fa
 
 // lib/types/icns.ts
 var SIZE_HEADER = 4 + 4;
+var MIN_ENTRY_LENGTH = 8;
 var FILE_LENGTH_OFFSET = 4;
 var ENTRY_LENGTH_OFFSET = 4;
 var ICON_TYPE_SIZE = {
@@ -61,6 +62,7 @@ function getImageSize(type) {
   const size = ICON_TYPE_SIZE[type];
   return { width: size, height: size, type };
 }
+var MIN_ENTRY_LENGTH = 8;
 var ICNS = {
   validate: (input) => toUTF8String(input, 0, 4) === "icns",
   calculate(input) {
@@ -69,10 +71,17 @@ var ICNS = {
     let imageOffset = SIZE_HEADER;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
+      if (inputLength - imageOffset < MIN_ENTRY_LENGTH) break;
       const imageHeader = readImageHeader(input, imageOffset);
+      const entryLength = imageHeader[1];
+      if (entryLength < MIN_ENTRY_LENGTH) break;
       const imageSize = getImageSize(imageHeader[0]);
-      images.push(imageSize);
-      imageOffset += imageHeader[1];
+      if (imageSize.width && imageSize.height) {
+        images.push(imageSize);
+      }
+      const nextOffset = imageOffset + entryLength;
+      if (nextOffset <= imageOffset) break;
+      imageOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
diff --git a/dist/types/index.cjs b/dist/types/index.cjs
index 6949cd134db7ab59dae7b804b36c7bacb4e387cf..2a977f67061fbf6ef0b0f07f0997c5f3e97c67b3 100644
--- a/dist/types/index.cjs
+++ b/dist/types/index.cjs
@@ -27,22 +27,36 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   const methodName = `readUInt${bits}${endian}`;
   return methods[methodName](input, offset);
 }
+const MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
diff --git a/dist/types/index.mjs b/dist/types/index.mjs
index b026dda4aa16c6077a76e4b3cfe3f54fe1d6d1d0..2f6976fa137b303cc26095bc12a74bdbe839fbe9 100644
--- a/dist/types/index.mjs
+++ b/dist/types/index.mjs
@@ -25,22 +25,36 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   const methodName = `readUInt${bits}${endian}`;
   return methods[methodName](input, offset);
 }
+const MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
diff --git a/dist/types/jp2.cjs b/dist/types/jp2.cjs
index cdd7c884efeb940b99f69a8f1d6ed361a1979211..69909d38f2b084f7c3dee827afbfb992b9eadda9 100644
--- a/dist/types/jp2.cjs
+++ b/dist/types/jp2.cjs
@@ -5,22 +5,36 @@ var decoder = new TextDecoder();
 var toUTF8String = (input, start = 0, end = input.length) => decoder.decode(input.slice(start, end));
 var getView = (input, offset) => new DataView(input.buffer, input.byteOffset + offset);
 var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, false);
+const MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
@@ -36,8 +50,8 @@ var JP2 = {
   },
   calculate(input) {
     const jp2hBox = findBox(input, "jp2h", 0);
-    const ihdrBox = jp2hBox && findBox(input, "ihdr", jp2hBox.offset + 8);
-    if (ihdrBox) {
+    const ihdrBox = jp2hBox && jp2hBox.size >= 8 && findBox(input, "ihdr", jp2hBox.offset + 8);
+    if (ihdrBox && ihdrBox.size >= 8) {
       return {
         height: readUInt32BE(input, ihdrBox.offset + 8),
         width: readUInt32BE(input, ihdrBox.offset + 12)
diff --git a/dist/types/jp2.mjs b/dist/types/jp2.mjs
index 63a537635b4f91250431a53225d4521ed60cf67d..263a86a65f4f3d9ef1910509f3e2b414a22ac46b 100644
--- a/dist/types/jp2.mjs
+++ b/dist/types/jp2.mjs
@@ -3,22 +3,36 @@ var decoder = new TextDecoder();
 var toUTF8String = (input, start = 0, end = input.length) => decoder.decode(input.slice(start, end));
 var getView = (input, offset) => new DataView(input.buffer, input.byteOffset + offset);
 var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, false);
+const MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
@@ -34,8 +48,8 @@ var JP2 = {
   },
   calculate(input) {
     const jp2hBox = findBox(input, "jp2h", 0);
-    const ihdrBox = jp2hBox && findBox(input, "ihdr", jp2hBox.offset + 8);
-    if (ihdrBox) {
+    const ihdrBox = jp2hBox && jp2hBox.size >= 8 && findBox(input, "ihdr", jp2hBox.offset + 8);
+    if (ihdrBox && ihdrBox.size >= 8) {
       return {
         height: readUInt32BE(input, ihdrBox.offset + 8),
         width: readUInt32BE(input, ihdrBox.offset + 12)
diff --git a/dist/types/jpg.cjs b/dist/types/jpg.cjs
index 664da4aaed5404e3637bf39ac5a58990be473b48..78b3c1e75b827c2408e831a49fa87f80083c2201 100644
--- a/dist/types/jpg.cjs
+++ b/dist/types/jpg.cjs
@@ -89,6 +89,9 @@ var JPG = {
     let next;
     while (input.length) {
       const i = readUInt16BE(input, 0);
+      if (i < 2) {
+        throw new TypeError("Corrupt JPG, invalid segment length");
+      }
       validateInput(input, i);
       if (input[i] !== 255) {
         input = input.slice(1);
diff --git a/dist/types/jpg.mjs b/dist/types/jpg.mjs
index 8fd607609035b43e7c1cc6fbe8d041f737532161..95fdb6b3e99313868437fb0136abd080e936b0e7 100644
--- a/dist/types/jpg.mjs
+++ b/dist/types/jpg.mjs
@@ -87,6 +87,9 @@ var JPG = {
     let next;
     while (input.length) {
       const i = readUInt16BE(input, 0);
+      if (i < 2) {
+        throw new TypeError("Corrupt JPG, invalid segment length");
+      }
       validateInput(input, i);
       if (input[i] !== 255) {
         input = input.slice(1);
diff --git a/dist/types/jxl.cjs b/dist/types/jxl.cjs
index a12095be85c7a8fd25763895a47c46b4292ebd9c..fe6972acc5806ed72b2b029d603c07c784c80bed 100644
--- a/dist/types/jxl.cjs
+++ b/dist/types/jxl.cjs
@@ -46,22 +46,36 @@ var toUTF8String = (input, start = 0, end = input.length) => decoder.decode(inpu
 var toHexString = (input, start = 0, end = input.length) => input.slice(start, end).reduce((memo, i) => memo + `0${i.toString(16)}`.slice(-2), "");
 var getView = (input, offset) => new DataView(input.buffer, input.byteOffset + offset);
 var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, false);
+const MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
@@ -101,7 +115,7 @@ var JXLStream = {
 // lib/types/jxl.ts
 function extractCodestream(input) {
   const jxlcBox = findBox(input, "jxlc", 0);
-  if (jxlcBox) {
+  if (jxlcBox && jxlcBox.size >= 8) {
     return input.slice(jxlcBox.offset + 8, jxlcBox.offset + jxlcBox.size);
   }
   const partialStreams = extractPartialStreams(input);
@@ -116,10 +130,13 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size < 12) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    const nextOffset = jxlpBox.offset + jxlpBox.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
   return partialStreams;
 }
diff --git a/dist/types/jxl.mjs b/dist/types/jxl.mjs
index 49b0c4cac6abb366378e5feed745f16981b441c0..2cb738b10fef2646beffc15f2f506208b36d0b05 100644
--- a/dist/types/jxl.mjs
+++ b/dist/types/jxl.mjs
@@ -44,22 +44,36 @@ var toUTF8String = (input, start = 0, end = input.length) => decoder.decode(inpu
 var toHexString = (input, start = 0, end = input.length) => input.slice(start, end).reduce((memo, i) => memo + `0${i.toString(16)}`.slice(-2), "");
 var getView = (input, offset) => new DataView(input.buffer, input.byteOffset + offset);
 var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, false);
+const MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
@@ -99,7 +113,7 @@ var JXLStream = {
 // lib/types/jxl.ts
 function extractCodestream(input) {
   const jxlcBox = findBox(input, "jxlc", 0);
-  if (jxlcBox) {
+  if (jxlcBox && jxlcBox.size >= 8) {
     return input.slice(jxlcBox.offset + 8, jxlcBox.offset + jxlcBox.size);
   }
   const partialStreams = extractPartialStreams(input);
@@ -114,10 +128,13 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size < 12) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    const nextOffset = jxlpBox.offset + jxlpBox.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
   return partialStreams;
 }
diff --git a/dist/types/utils.cjs b/dist/types/utils.cjs
index 23d52e4cbf271e667feb2d7f040c07f69678d3aa..8274447b64e15bd7673df382ddcacbcf8c73d502 100644
--- a/dist/types/utils.cjs
+++ b/dist/types/utils.cjs
@@ -27,22 +27,36 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   const methodName = `readUInt${bits}${endian}`;
   return methods[methodName](input, offset);
 }
+const MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
diff --git a/dist/types/utils.mjs b/dist/types/utils.mjs
index 3f09dceb036e40be8538dbb5588d88c951339158..8b38cd38a1a46d094fa1a28989ccc990543da886 100644
--- a/dist/types/utils.mjs
+++ b/dist/types/utils.mjs
@@ -25,22 +25,36 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   const methodName = `readUInt${bits}${endian}`;
   return methods[methodName](input, offset);
 }
+const MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
